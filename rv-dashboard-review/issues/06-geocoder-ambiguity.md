**UX — the route planner silently trusts the geocoder's first guess. Show what it picked (it's already computed!), or offer choices.**

`RVWeather.geocode()` asks Open-Meteo's geocoder for `count=1` and takes the top hit. Type "Springfield", "Columbus", "Portland", or "Greenville" and the app may happily plan a route to the wrong state — and nothing on screen ever says which one it chose. The status line shows distance and hours, so a wildly wrong distance is the only clue.

The ironic part: `geocode()` already builds a nice display name —

```js
return { lat, lon, name: `${r.name}${r.admin1 ? ", " + r.admin1 : ""}` };
```

— and `app.js` never uses `dest.name`.

### Minimum fix (two lines)

Put the resolved place into the status line so a wrong guess is at least visible:

```js
routeStatus.textContent =
  `To ${dest.name}: ${miles} mi • about ${hours} hr driving • departing ...`;
```

### Better fix

Request `count=5` and, when more than one result comes back, render them as tappable choices ("Springfield, Missouri / Springfield, Illinois / Springfield, Massachusetts…") before planning. It's a small amount of DOM code in the existing `route-status` area, and it turns the planner's most confusing failure mode into a one-tap disambiguation.

Bonus polish while in there: the geocoder also returns `admin1` and `country_code` — filtering or de-prioritizing non-US results would fit this app's audience.

---
_Generated by [Claude Code](https://claude.ai/code)_
